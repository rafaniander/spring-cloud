server:
  port: 8888
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: /home/rafael/Desktop/Git/rafaniander/spring-cloud/repository
          #uri: /home/rafael/Projetos/rafaniander/spring-cloud/repository
# refresh
management:
  endpoint:
    refresh:
      enabled: true